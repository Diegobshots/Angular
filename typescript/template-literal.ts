(function () {
  
    function getEdad() {
        return 100 + 100 + 300;
    }
    const nombre = 'Diego';
    const apellido = 'Blanco';
    const edad = 25;

    const salida = `${nombre} ${apellido} (${ getEdad() })`;

    console.log(salida);
    
}) ();



